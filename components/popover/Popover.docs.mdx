import { ArgTypes, Meta, Stories } from '@storybook/blocks'

import {
  PopoverArrow,
  PopoverContent,
  PopoverRoot,
  PopoverTrigger,
} from '@/components/popover'

import * as PopoverStories from './Popover.stories'

<Meta of={PopoverStories} />

# Popover

```tsx
import {
  PopoverArrow,
  PopoverContent,
  PopoverRoot,
  PopoverTrigger,
} from '@/components/popover'
```

## PopoverArrow Props

<ArgTypes of={PopoverArrow} />

## PopoverContent Props

<ArgTypes of={PopoverContent} />

## PopoverRoot Props

<ArgTypes of={PopoverRoot} />

## PopoverTrigger Props

<ArgTypes of={PopoverTrigger} />


<Stories />
