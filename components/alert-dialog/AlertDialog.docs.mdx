import { ArgTypes, Meta, Stories } from '@storybook/blocks'

import {
  AlertDialog,
  AlertDialogCancel,
  AlertDialogCloseButton,
  AlertDialogContent,
  AlertDialogFooter,
} from '@/components/alert-dialog'

import * as AlertDialogStories from './AlertDialog.stories'

<Meta of={AlertDialogStories} />

# AlertDialog

```tsx
import {
  AlertDialog,
  AlertDialogCancel,
  AlertDialogCloseButton,
  AlertDialogContent,
  AlertDialogFooter,
} from '@/components/alert-dialog'
```

## AlertDialog Props

<ArgTypes of={AlertDialog} />

## AlertDialogContent Props

<ArgTypes of={AlertDialogContent} />

## AlertDialogCancel Props

<ArgTypes of={AlertDialogCancel} />

## AlertDialogCloseButton Props

<ArgTypes of={AlertDialogCloseButton} />

## AlertDialogFooter Props

<ArgTypes of={AlertDialogFooter} />

<Stories />
